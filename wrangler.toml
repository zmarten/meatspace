name = "meatspace"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# Non-secret environment variables.
# Secrets (API keys, tokens) are added via the Cloudflare dashboard → Settings → Variables → Secrets.
# For local `wrangler pages dev` testing, copy .env.local values into .dev.vars instead.
# https://developers.cloudflare.com/pages/functions/bindings/#environment-variables
[vars]
NOTIFICATION_EMAIL = "zachdmartens@gmail.com"
NOTIFICATION_FROM = "MeatSpace <noreply@meatspace.run>"
