{
  "schema_version": "1.0",
  "name": "meatspace",
  "display_name": "MeatSpace",
  "description": "Flesh-in-the-loop service for agents. Use provision_api_key to get a Bearer token, then ask_human for subjective choices.",
  "version": "0.1.0",
  "transport": {
    "type": "http",
    "url": "https://meatspace.run/api/mcp"
  },
  "tools": [
    {
      "name": "get_service_status",
      "description": "Check whether MeatSpace is available and when to escalate to a human. No auth required.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "provision_api_key",
      "description": "Create a MeatSpace API key instantly. No auth required. Returns a Bearer token for ask_human. Max 5 keys per email.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "description": "Agent or tool name (max 100 chars)" },
          "email": { "type": "string", "description": "Owner email address" }
        },
        "required": ["name", "email"]
      }
    },
    {
      "name": "ask_human",
      "description": "Submit content plus 2-4 choices for subjective human judgment, approval, preference, or tie-breaks. Requires Bearer auth.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "agent_name": { "type": "string", "description": "Your agent or tool name" },
          "title": { "type": "string", "description": "Short title shown to the human reviewer" },
          "content": { "type": "string", "description": "Optional review content" },
          "content_type": { "type": "string", "enum": ["text", "markdown", "html", "image"] },
          "choices": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": { "type": "string" },
                "label": { "type": "string" }
              },
              "required": ["id", "label"]
            },
            "minItems": 2,
            "maxItems": 4
          },
          "decision_reason": { "type": "string" },
          "confidence": { "type": "number" },
          "consequence_of_wrong_choice": { "type": "string" },
          "recommended_option": { "type": "string" },
          "run_id": { "type": "string" },
          "trace_id": { "type": "string" },
          "callback_url": { "type": "string" },
          "metadata": { "type": "object" },
          "timeout_seconds": { "type": "number" }
        },
        "required": ["agent_name", "title", "choices"]
      }
    }
  ],
  "resources": [
    {
      "name": "service_info",
      "description": "Full MeatSpace agent documentation",
      "uri": "https://meatspace.run/llms-full.txt",
      "mimeType": "text/plain"
    },
    {
      "name": "browser_sdk",
      "description": "MeatSpace JavaScript SDK for browser-based agents",
      "uri": "https://meatspace.run/sdk/meatspace.js",
      "mimeType": "application/javascript"
    }
  ]
}
